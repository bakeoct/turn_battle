package Calc.save;

import Calc.Person2;
import Calc.Position;
import Monsters.Monster2;

import java.time.Period;
import java.util.ArrayList;

public class InSave {
    public InSave(ArrayList<String> String_read_memory,ArrayList<Integer> Int_read_memory, Position monsterposition, Position position, Person2 p, ArrayList<Monster2> monsters2){
        p.name = memory.get(0);
        p.seibetu = memory.get(1);
        p = (int)memory.get(2);
    }
}
